git clone -d branch origin 
git add .
git add [ファイル名]
git commit -m "コメント"
git push origin branch
git checkout -b
git checkout -d
git checkout 
git reset
git fetch

※origin ＝　URLでOK
対象のフォルダは最初に初期化する必要あり